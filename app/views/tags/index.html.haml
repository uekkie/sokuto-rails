-# TODO: 4*9で並べる
-# TODO: タグ名で絞り込む機能
-# TODO: 新規、名前、人気の一覧ができる
%ul.list-group
  - @tags.each do |tag|
    %li.list-group-item
      =link_to tag.name,
      { tag_name: tag.name, controller: "questions", action: "tagged" }
